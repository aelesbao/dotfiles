session: dotfiles
root: ~

windows:
  - name: "\U000F02DC ~"
  - name: top
    commands: [htop]
    panes:
      - commands: [command zenith || exit 0]
  - name: git
    commands: [lazygit -g ~/.local/share/yadm/repo.git -w ~]
