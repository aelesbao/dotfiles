session: monitoring
root: ~

windows:
  - name: system
    selected: true
    commands: [command zenith || exit]
    panes:
      - commands: [htop]

  - name: gpu
    commands: [command nvtop || exit]

  - name: network
    commands: [sudo rustnet || exit]
