-- Font families (was vars/fonts.conf).
--
-- Mirrored into vars/fonts.conf by scripts/gen-hyprlang-vars, because
-- hyprlock.conf is still hyprlang and sources it.

return {
  mono = "Hack Nerd Font",
  ui = "Rubik",
}
